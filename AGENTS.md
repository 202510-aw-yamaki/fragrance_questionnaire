# AGENTS.md

## 0. このリポジトリでのCodexの役割

このリポジトリでは、ユーザーが主体者であり、Codex は調査・計画・実装・確認を補助するコーディングエージェントである。

Codex は、ユーザーの指示をもとに、リポジトリを調査し、実装計画を立て、承認済み範囲のコード変更・確認・ローカルコミットまで進めてよい。

ただし、push、PR作成、破壊的操作、仕様未確定領域の確定実装は、ユーザーの明示承認なしに行ってはいけない。

---

## 1. Codex app / Windows 前提

Codex app は Windows 環境でも使用される前提で作業する。

Windows や PowerShell では、日本語が文字化けして見える場合がある。  
文字化けに見えても、ファイル本体が壊れていると決めつけない。

守ること。

- UTF-8 BOM を付けない。
- 文字化け疑い箇所を勝手に修正しない。
- 日本語ファイル名・日本語ディレクトリ名を勝手に変更しない。
- 改行コードや文字コードの一括変換をしない。
- PowerShell表示由来の問題を、コード破損として扱わない。

---

## 2. 作業前に読む正本

通常作業では、以下を現在の正本として扱う。

1. `AGENTS.md`
2. `docs/00_PROJECT_CORE.md`
3. `docs/01_CURRENT_STATE.md`
4. `docs/02_TARGET_ARCHITECTURE.md`
5. `docs/03_DB_DESIGN_POLICY.md`
6. `docs/04_QR_PRODUCT_FLOW.md`
7. `docs/05_IMPLEMENTATION_ROADMAP.md`
8. `docs/06_OPEN_ISSUES.md`
9. `docs/SPEC.md`
10. `docs/SURVEY_SCORING_LOGIC.md`
11. `docs/MATERIAL_POINTS.md`

DB・認証・予約・QR商品導線・通知・期限管理に関わる作業では、特に以下を必ず確認する。

- `docs/03_DB_DESIGN_POLICY.md`
- `docs/04_QR_PRODUCT_FLOW.md`
- `docs/05_IMPLEMENTATION_ROADMAP.md`
- `docs/06_OPEN_ISSUES.md`

---

## 3. 明示承認なしで可能な操作

以下は、ユーザーの追加承認なしで実行してよい。

- プロジェクト内のファイルを読む。
- `AGENTS.md` と設計資料を確認する。
- `rg` などで関連箇所を調査する。
- 現状監査を行う。
- 影響範囲を整理する。
- 実装計画を作る。
- 非破壊の確認コマンドを実行する。

例。

- `git status`
- `git diff`
- `git log`
- `rg`
- `Get-ChildItem`
- `Get-Content`
- ファイル内容確認
- 構文確認
- 参照パス確認

---

## 4. 実装承認の扱い

ユーザーが次のように依頼した場合、そのタスク範囲内のファイル編集は承認済みとして扱う。

- 「実装して」
- 「修正して」
- 「進めて」
- 「着手して」
- 「この方針でやって」
- 「Planモードで進めて」
- 「QR周りを実装まで持っていって」

この場合、Codex は承認済み範囲内で、必要な一部置換・局所修正・関連ドキュメント更新・確認・小さなローカルコミットまで進めてよい。

ただし、承認済み範囲を超える変更が必要になった場合は止まって確認する。

---

## 5. 明示承認が必要な操作

以下は、ユーザーの明示承認なしに行ってはいけない。

- 1ファイル全体の置換。
- 複数ファイルの一括置換。
- 文字化け疑い箇所の修正。
- DBスキーマの破壊的変更。
- Supabase本番環境への変更。
- secret / key / token の追加・表示・保存。
- package の大量追加・更新。
- lock file の意図しない更新。
- Git履歴の書き換え。
- rebase。
- reset。
- force push。
- push。
- PR作成。
- 外部決済など未確定仕様の実装。

---

## 6. Planモードでの進め方

Planモードでは、まず調査して実装計画を作る。

ただし、ユーザーが「実装まで」「進めて」「着手して」と依頼している場合は、Plan作成後に必要以上に停止せず、承認済み範囲の実装へ進んでよい。

Planモードで行うこと。

1. 正本資料を読む。
2. 関連コードを調査する。
3. 既存実装と設計の差分を整理する。
4. 小さな実装単位に分ける。
5. リスクがある箇所を明示する。
6. 承認済み範囲は実装する。
7. 変更後に確認する。
8. 必要なら関連ドキュメントを更新する。
9. 小さくローカルコミットする。

Planモードでも、以下は止まって確認する。

- 仕様未確定事項を確定する必要がある場合。
- 1ファイル全体置換が必要な場合。
- 複数ファイルの一括置換が必要な場合。
- DB破壊的変更が必要な場合。
- push または PR作成が必要な場合。
- 既存導線を壊す可能性が高い場合。

---

## 7. 現在のリポジトリ状態

2026-05-06 時点では、QR導線以外の現状実装を現在の正本として扱う。

現行実装の主な対象。

- `index.html`
- `customer/`
- `staff/`
- `admin/`
- `assets/`
- `supabase/`
- `scripts/`
- `tests/`

QR導線は削除・過去資料化せず、後続課題として維持する。

公開用整理後、過去資料・旧実装ディレクトリは本リポジトリに含めない。

必要な場合のみ、別保管の移行元資料を現在の正本と照合して参照する。

---

## 8. プロジェクトの芯

このプロジェクトは、香りワークショップ向けの予約・アンケート・スタッフ管理・管理者設定・QR商品導線システムである。

最優先は以下。

- アンケート回答
- 香り5軸の算出
- アンケート結果保存
- 来店予約
- スタッフ運用
- 管理者によるロジック調整
- 管理者による原料ポイント調整
- Supabase連携の安定化

追加価値として、ワークショップで完成した香水をQR商品として第三者が作成依頼できる導線を整備する。

---

## 9. 絶対に守るルール

1. UIだけを先に作り、DB・認証・状態管理を後回しにしない。
2. QR商品導線と会員導線を混ぜない。
3. QRから来た第三者を会員DBに入れない。
4. QRコードは「元のお客様の完成品」に紐づける。
5. QR商品依頼は「購入」ではなく「作成依頼」として扱う。
6. 管理者・スタッフ・会員の権限を混在させない。
7. DB変更時は必ず migration / schema / RPC / RLS / 画面影響を確認する。
8. Supabase anon key のみを client 側で扱い、service role key は client 側で使わない。
9. 既存の予約・アンケート・スタッフ確認導線を壊さない。
10. 実装後は、関連ドキュメントも必要に応じて更新する。

---

## 10. 現在完了済みとして扱うもの

以下は、未着手タスクとして扱わない。

- UI再構築
- `customer/product-reservation.html` のQR商品作成依頼ページ化
- QR依頼保存の入口
- QR受付通知・メールイベントの入口
- QRアクセス記録の入口
- スタッフ/管理者向けQR依頼の読み取り用一覧
- `staff-customer-detail.html` の商品名入力
- 個人情報同意・第三者作成同意の保存入口
- スタッフ画面の事前配合調整
- 商品タグ選択モーダル
- 予約者情報の保存
- 予約枠ベースの公開予約
- `create_public_reservation()` RPC による過予約防止
- 予約作成時の予約通知
- スタッフダッシュボードでの予約通知表示
- 管理者ダッシュボードのスタッフ別運用サマリー

ただし、完了済み項目でも Supabase 実接続での結合確認が未完了の場合は、確認対象として扱う。

---

## 11. 現在の優先作業

### 11.1 Supabase実接続での結合確認

確認対象。

- 顧客ログイン
- 会員トップ
- アンケート結果保存
- 予約作成
- スタッフログイン
- 予約一覧
- 予約枠作成
- 顧客詳細保存
- 管理者ログイン
- 基本設定
- 配点
- 原料ポイント
- QR依頼一覧
- QR商品作成依頼の表示
- QR商品作成依頼の数量入力
- QR商品作成依頼のメールアドレス入力
- QR商品作成依頼の依頼保存

### 11.2 QR商品作成依頼の運用処理

実装対象。

- スタッフによる作成可能 / 作成不可判断
- 受付メール
- 作成可能メール
- 作成不可メール
- 再案内メール
- 期限切れメール
- 3営業日以内の判断期限
- 期限超過未対応の管理者記録
- 管理者側の通知・集計

### 11.3 発送先入力・発送完了・成果集計

実装対象。

- 発送先入力
- 発送完了登録
- 発送完了時点でのスタッフ成果Bカウント
- 作成可能メール送信後3日後の再案内
- 作成可能メール送信後7日後の期限切れ

ただし、発送先入力は個人情報保持・削除方針と関係するため、実装前に仕様確認する。

### 11.4 会員ページ整理

実装対象。

- 会員登録DB接続の結合確認
- 会員ページでの前回完成品表示
- アンケート経由予約時の前回完成品との差分表示
- 予約履歴・制作履歴と完成品の紐づけ確認

### 11.5 管理者設定・通知・期限管理

実装対象。

- QR商品価格設定の実運用確認
- 通知イベントとメールイベントの状態管理
- 期限超過集計
- QR無効化
- アクセス急増通知
- 管理者集計

---

## 12. QR商品導線ルール

QR商品導線では、以下を守る。

- QR商品ページでは商品名のみ表示する。
- 表示する入力項目は、10ml数量、30ml数量、メールアドレス。
- ボタン文言は「この香水を作成依頼する」。
- QR商品依頼は購入ではなく作成依頼として扱う。
- QR経由の第三者を会員DBに入れない。
- スタッフは3営業日以内に作成可能 / 作成不可を判断する。
- 3営業日を超えて未対応の場合は、作成不可または期限超過扱いをシステム上で記録する。
- 作成可能メール送信後、3日後に再案内、7日後に期限切れとする。
- 発送完了時点でスタッフ成果Bとしてカウントする。
- QR商品ページは最後の発送完了から半年注文がなければ無効化する。
- 無効化後にアクセスが多い場合は、管理者通知に出す。

---

## 13. 予約・スタッフ運用ルール

予約保存は `create_public_reservation()` RPC を正本として扱う。

予約枠 `slot_id` がある場合は、DB側で対象予約枠をロックしてから保存する。

予約可能条件。

- 有効枠である。
- 受付中ステータスである。
- 過去日時ではない。
- `canceled` 以外の予約数が `reservation_slots.capacity` 未満である。

スタッフ予約一覧は、ログイン中スタッフの `staff_profiles.id` に紐づく予約だけを表示する。

判定優先順位。

1. `reservations.staff_profile_id`
2. `reservation_slots.staff_profile_id`
3. 既存データ救済用に限り、`reservation_slots.instructor_name` とログインスタッフ名の一致

スタッフプロフィール未登録時は予約一覧を表示せず、管理者への紐づけ依頼を出す。

---

## 14. CSS・画面構成ルール

- 顧客系の共通UIは `assets/css/rebuild-ui.css` を中心に扱う。
- 管理者系の見た目調整は `assets/css/admin/admin-ui.css` に集約する。
- スタッフ系の見た目調整は `assets/css/staff-ui.css` に集約する。
- staff 配下のページは原則として `assets/css/rebuild-ui.css` を参照しない。
- admin 配下の管理者ページと `admin/login.html` は、admin 系スタイルを `assets/css/admin/admin-ui.css` に寄せる。
- ページ個別の巨大な inline CSS / inline JS を増やさない。

---

## 15. DB変更ルール

DB変更時は、以下を必ず確認する。

- migration ファイル
- `supabase/schema.sql`
- RPC
- RLS
- 既存データへの影響
- client 側 JS への影響
- 関連画面への影響
- 関連ドキュメントへの影響

DB変更だけ、または画面変更だけで終わらせない。  
保存・取得・権限・表示まで一連で確認する。

---

## 16. Git / commit ルール

ユーザー承認済みの実装作業が完了した適切な区切りで、関連変更だけを小さくローカルコミットしてよい。

ただし、push と PR作成はユーザーの明示指示があるまで行わない。

コミット前に確認する。

- `git diff`
- `git status`
- 意図しないファイル変更がないこと
- 文字コード変更が混ざっていないこと
- 改行コードの一括変更が混ざっていないこと
- 関連ドキュメント更新漏れがないこと

---

## 17. テスト・確認

このリポジトリには Playwright 設定がある。  
ただし `package.json` の scripts が空の場合、存在しない npm script を実行しない。

可能な範囲で確認する。

- HTMLの参照パス
- JSの構文
- 主要ページの表示
- Supabase RPC / table 名の整合
- 予約・QR・会員・スタッフ・管理者導線への影響
- `git diff` による意図しない変更

自動テストが実行できない場合は、その理由を報告する。

---

## 18. 報告形式

作業完了時は、以下を簡潔に報告する。

- 何を確認したか
- 何を変更したか
- どのファイルを変更したか
- 実行したテスト・確認
- 実行できなかった確認と理由
- ローカルコミットを作成した場合は、その概要
- 残課題
- 次に行うべき作業

---

## 19. 判断に迷った場合

判断に迷った場合は、以下を優先する。

1. 既存機能を壊さない。
2. 現在の正本を優先する。
3. 未確定事項を勝手に仕様化しない。
4. DB・認証・状態管理を軽視しない。
5. QR導線と会員導線を混ぜない。
6. 大規模変更より局所修正を優先する。
7. 文字化けに見える箇所を勝手に直さない。
8. 調査・Planは進めてよい。
9. 承認済みタスク範囲の実装は止まりすぎず進める。

# Codexへの指示書 (AGENTS.md)

## 0. このファイルの目的

このファイルは、Codex app / Codex CLI / Codex IDE / Codex web が、このリポジトリで安全に作業するための共通指示書である。

このリポジトリでは、ユーザーが主体者であり、Codex は実装・調査・修正・確認を補助するコーディングアシスタントである。

Codex は、ユーザーの意図を読み取り、既存方針に沿って実装へ落とし込む。  
ただし、このリポジトリでは **調査・確認は承認なしで進めてよいが、ファイル編集はユーザーの明示承認後に行う**。

---

## 1. Codex app / Windows 作業前提

Codex app は Windows 環境でも使用される前提で作業する。

Windows では、PowerShell・ターミナル・エディタ・Git の表示差異により、日本語が文字化けして見える場合がある。  
文字化けに見える内容があっても、すぐにファイルを修正してはいけない。  
まず表示環境・エンコード・PowerShell表示の問題を疑い、必要ならユーザーに確認する。

### Windows作業時の注意

- UTF-8 BOM を付けない。
- 日本語ファイル名・日本語ディレクトリ名を勝手に英数字へ変更しない。
- パス区切りは、コード内では既存の書き方に合わせる。
- PowerShell表示で文字化けしても、ファイル本体が壊れていると決めつけない。
- 文字化け疑い箇所を、ユーザー確認なしに修正しない。
- 一括エンコード変換を勝手に実行しない。
- 改行コードの一括変換を勝手に行わない。
- Windows固有の一時ファイルやエディタ生成物をコミットしない。

---

## 2. Codex app の並列エージェント運用

Codex app では複数エージェントが並列に作業できる。  
そのため、このリポジトリでは以下を守る。

- 1タスク = 1つの明確な目的に絞る。
- 1エージェントが担当する変更範囲を小さくする。
- 複数エージェントで同じファイルを同時に大きく編集しない。
- worktree やブランチを使う場合でも、現在の正本と衝突しないように確認する。
- 既存PR・既存ブランチがある場合は、作業前に内容を確認する。
- 古いCodex作業ブランチの大規模差分は、そのままマージせず、必要部分だけ拾う。
- 並列作業後は、変更内容・影響範囲・残課題を明確に報告する。

---

## 3. 明示承認なしで可能な操作

以下は、ユーザーの明示承認なしで実行してよい。

### 調査・確認

- プロジェクト内のファイルを読むこと。
- `AGENTS.md` と指定された設計資料を作業前に確認すること。
- `rg` などでコード・仕様・関連箇所を調査すること。
- 現状監査を行うこと。
- 関連ファイルの依存関係を確認すること。
- 既存仕様と実装のズレを整理すること。
- 既存PR・ブランチ・差分を確認すること。

### 非破壊コマンド

以下のような、ファイルを変更しない非破壊の確認コマンドは実行してよい。

- `git status`
- `git diff`
- `git log`
- `rg`
- `Get-ChildItem`
- `Get-Content`
- `ls`
- `cat`
- ファイル内容確認
- 構文確認
- 参照パス確認
- ローカルで完結する軽量な検証

### 作業完了後のローカルコミット

実装作業がユーザー承認済みであり、作業が完了した適切な区切りであれば、関連変更だけを小さくローカルコミットしてよい。

ただし、以下を守る。

- push は不可。
- PR作成は不可。
- 1コミットは1つの意味のある変更に限定する。
- 意図しないファイル変更を含めない。
- 文字コード変更や改行コード変更を混ぜない。
- コミット前に `git diff` と `git status` を確認する。
- コミット内容を報告する。

### 実装後の関連ドキュメント更新

実装がユーザー承認済みであり、その実装に関連する範囲であれば、関連ドキュメントを更新してよい。

ただし、ドキュメント更新も実装承認の範囲内に限る。  
仕様未確定の内容を、勝手に確定仕様として記載してはいけない。

---

## 4. 明示承認が必要な操作

以下は、ユーザーの明示承認なしに行ってはいけない。

### ファイル編集

- ファイルの一部置換。
- 1ファイル内を含む全体置換。
- 複数ファイルの一括置換。
- 文字化け疑い箇所の修正。
- 文字コード変更。
- 改行コードの一括変更。
- ファイル名・ディレクトリ名の変更。
- 既存HTML / CSS / JS の大規模整理。
- Markdown の大幅な構成変更。
- 画像・PPTX・PDFなど大きなバイナリの追加・削除。

### Git操作

- push。
- PR作成。
- main ブランチへの直接反映。
- Git履歴の書き換え。
- rebase。
- reset。
- force push。
- branch削除。
- tag作成・削除。

### 破壊的操作

- ファイルやディレクトリの削除。
- 複数ファイルの大量移動。
- DBスキーマの破壊的変更。
- Supabase本番環境への変更。
- secret / key / token の追加・表示・保存。
- package の大量追加・更新。
- lock file の意図しない更新。
- 外部サービス連携の確定。
- ネットワークアクセスが必要なコマンド。
- 仕様未確定の機能実装。

---

## 5. 作業モードの考え方

このリポジトリでは、Codex の作業を次の3段階で扱う。

### 5.1 調査モード

調査モードでは、明示承認なしで以下を行ってよい。

- ファイル読込
- 設計資料確認
- `rg` による検索
- `git status`
- `git diff`
- 現状監査
- 影響範囲の整理
- 実装方針の提案

この段階では、ファイル編集を行ってはいけない。

### 5.2 実装モード

実装モードに入るには、ユーザーの明示承認が必要である。

実装モードでは、承認された範囲内でのみファイルを編集してよい。  
承認範囲を超える修正が必要になった場合は、作業を止めて確認する。

### 5.3 完了処理モード

実装完了後、以下を行ってよい。

- 関連する確認コマンドの実行
- `git diff` / `git status` の確認
- 関連ドキュメントの更新
- 小さなローカルコミット
- 変更内容・確認結果・残課題の報告

ただし、push と PR作成は不可。

---

## 6. 現在の正本

通常作業では、以下を現在の正本として扱う。

1. `AGENTS.md`
2. `docs/00_PROJECT_CORE.md`
3. `docs/01_CURRENT_STATE.md`
4. `docs/02_TARGET_ARCHITECTURE.md`
5. `docs/03_DB_DESIGN_POLICY.md`
6. `docs/04_QR_PRODUCT_FLOW.md`
7. `docs/05_IMPLEMENTATION_ROADMAP.md`
8. `docs/06_OPEN_ISSUES.md`
9. `docs/SPEC.md`
10. `docs/SURVEY_SCORING_LOGIC.md`
11. `docs/MATERIAL_POINTS.md`

DB・認証・QR商品導線・通知・期限管理に関わる作業では、特に以下を必ず確認する。

- `docs/03_DB_DESIGN_POLICY.md`
- `docs/04_QR_PRODUCT_FLOW.md`
- `docs/05_IMPLEMENTATION_ROADMAP.md`
- `docs/06_OPEN_ISSUES.md`

---

## 7. 現在のリポジトリ状態

2026-05-06 時点では、QR導線以外の現状実装を現在の正本として扱う。

現行実装の主な対象は以下。

- `index.html`
- `customer/`
- `staff/`
- `admin/`
- `css/`
- `js/`
- `supabase/`
- `scripts/`
- `tests/`

QR導線は削除・過去資料化せず、後続課題として維持する。

旧資料・旧実装は以下に分離されている。

- 過去資料: `docs/archive/`
- 旧画面・旧コード実体: `archived/`

これらを現在仕様としてそのまま流用してはいけない。  
必要な場合のみ、現在の正本と照合して参照する。

---

## 8. プロジェクトの芯

このプロジェクトは、香りワークショップ向けの予約・アンケート・スタッフ管理・管理者設定・QR商品導線システムである。

最優先は以下。

- アンケート回答
- 香り5軸の算出
- アンケート結果保存
- 来店予約
- スタッフ運用
- 管理者によるロジック調整
- 管理者による原料ポイント調整
- Supabase連携の安定化

追加価値として、ワークショップで完成した香水をQR商品として第三者が作成依頼できる導線を整備する。

---

## 9. Non-Negotiable Rules

1. UIだけを先に作り、DB・認証・状態管理を後回しにしない。
2. QR商品導線と会員導線を混ぜない。
3. QRから来た第三者を会員DBに入れない。
4. QRコードは「元のお客様の完成品」に紐づける。
5. QR商品依頼は「購入」ではなく「作成依頼」として扱う。
6. 管理者・スタッフ・会員の権限を混在させない。
7. DB変更時は必ず migration / schema / RPC / RLS / 画面影響を確認する。
8. Supabase anon key のみを client 側で扱い、service role key は client 側で使わない。
9. 既存の予約・アンケート・スタッフ確認導線を壊さない。
10. 実装後は、関連ドキュメントも必要に応じて更新する。
11. 文字化けに見える箇所を、確認なしに修正しない。
12. 全体置換や大規模整理は、必ずユーザー確認後に行う。

---

## 10. 現在完了済みとして扱うもの

以下は、未着手タスクとして扱わない。

- UI再構築
- `docs/assets/layout-reference/01`〜`10`、`13`〜`20` の反映
- `customer/product-reservation.html` のQR商品作成依頼ページ化
- QR依頼保存の入口
- QR受付通知・メールイベントの入口
- QRアクセス記録の入口
- スタッフ/管理者向けQR依頼の読み取り用一覧
- `staff-customer-detail.html` の商品名入力
- 個人情報同意・第三者作成同意の保存入口
- スタッフ画面の事前配合調整
- 商品タグ選択モーダル
- 予約者情報の保存
- 予約枠ベースの公開予約
- `create_public_reservation()` RPC による過予約防止
- 予約作成時の予約通知
- スタッフダッシュボードでの予約通知表示
- 管理者ダッシュボードのスタッフ別運用サマリー

ただし、完了済み項目でも Supabase 実接続での結合確認が未完了の場合は、確認対象として扱う。

---

## 11. 現在の優先作業

### 11.1 Supabase実接続での結合確認

再構築後のHTMLが、実DB・認証・RPCと破綻なくつながるかを確認する。

確認対象。

- 顧客ログイン
- 会員トップ
- アンケート結果保存
- 予約作成
- スタッフログイン
- 予約一覧
- 予約枠作成
- 顧客詳細保存
- 管理者ログイン
- 基本設定
- 配点
- 原料ポイント
- QR依頼一覧
- QR商品作成依頼の表示
- QR商品作成依頼の数量入力
- QR商品作成依頼のメールアドレス入力
- QR商品作成依頼の依頼保存

### 11.2 QR商品作成依頼の運用処理

QR商品作成依頼は、購入ではなく「作成依頼」として扱う。

実装対象。

- スタッフによる作成可能 / 作成不可判断
- 受付メール
- 作成可能メール
- 作成不可メール
- 再案内メール
- 期限切れメール
- 3営業日以内の判断期限
- 期限超過未対応の管理者記録
- 管理者側の通知・集計

### 11.3 発送先入力・発送完了・成果集計

実装対象。

- 発送先入力
- 発送完了登録
- 発送完了時点でのスタッフ成果Bカウント
- 作成可能メール送信後3日後の再案内
- 作成可能メール送信後7日後の期限切れ

ただし、発送先入力は個人情報保持・削除方針と関係するため、実装前に仕様確認する。

### 11.4 会員ページ整理

会員導線とQR第三者導線を混ぜない。

実装対象。

- 会員登録DB接続の結合確認
- 会員ページでの前回完成品表示
- アンケート経由予約時の前回完成品との差分表示
- 予約履歴・制作履歴と完成品の紐づけ確認

### 11.5 管理者設定・通知・期限管理

実装対象。

- QR商品価格設定の実運用確認
- 通知イベントとメールイベントの状態管理
- 期限超過集計
- QR無効化
- アクセス急増通知
- 管理者集計

---

## 12. 未確定のため勝手に実装しない項目

以下は `docs/06_OPEN_ISSUES.md` の確認対象であり、ユーザー確認なしに実装してはいけない。

- 外部決済
- 送料の自動計算
- 同意文バージョン管理
- QR商品の再有効化
- 大量注文専用フォーム
- 会員ページの差分表示ロジックの確定
- スタッフ成果指標の確定集計
- QR依頼の個人情報保持・削除・匿名化運用
- 発送先入力ページの個人情報保存仕様
- QR商品設定ページの独立

---

## 13. QR Product Rules

QR商品導線では、以下を守る。

- QR商品ページでは商品名のみ表示する。
- 表示する入力項目は、10ml数量、30ml数量、メールアドレス。
- ボタン文言は「この香水を作成依頼する」。
- QR商品依頼は購入ではなく作成依頼として扱う。
- QR経由の第三者を会員DBに入れない。
- スタッフは3営業日以内に作成可能 / 作成不可を判断する。
- 3営業日を超えて未対応の場合は、作成不可または期限超過扱いをシステム上で記録する。
- 作成可能メール送信後、3日後に再案内、7日後に期限切れとする。
- 発送完了時点でスタッフ成果Bとしてカウントする。
- QR商品ページは最後の発送完了から半年注文がなければ無効化する。
- 無効化後にアクセスが多い場合は、管理者通知に出す。

---

## 14. 予約・スタッフ運用ルール

予約保存は `create_public_reservation()` RPC を正本として扱う。

予約枠 `slot_id` がある場合は、DB側で対象予約枠をロックしてから保存する。

予約可能条件は以下。

- 有効枠である
- 受付中ステータスである
- 過去日時ではない
- `canceled` 以外の予約数が `reservation_slots.capacity` 未満である

条件違反時は、顧客予約画面で完了画面へ進めず、枠の再選択を促す。

スタッフ予約一覧は、ログイン中スタッフの `staff_profiles.id` に紐づく予約だけを表示する。  
判定は以下を優先する。

1. `reservations.staff_profile_id`
2. `reservation_slots.staff_profile_id`
3. 既存データ救済用に限り、`reservation_slots.instructor_name` とログインスタッフ名の一致

スタッフプロフィール未登録時は予約一覧を表示せず、管理者への紐づけ依頼を出す。

---

## 15. CSS・画面構成ルール

- 顧客系の共通UIは `css/rebuild-ui.css` を中心に扱う。
- 管理者系の見た目調整は `admin/admin-ui.css` に集約する。
- スタッフ系の見た目調整は `css/staff-ui.css` に集約する。
- staff 配下のページは原則として `css/rebuild-ui.css` を参照しない。
- admin 配下の管理者ページと `admin-login.html` は、admin 系スタイルを `admin/admin-ui.css` に寄せる。
- ページ個別の巨大な inline CSS / inline JS を増やさない。
- 既存のCSS分離方針を崩す場合は、事前に理由を整理して確認する。

---

## 16. Supabase・DB変更ルール

DB変更時は、以下を必ず確認する。

- migration ファイル
- `supabase/schema.sql`
- RPC
- RLS
- 既存データへの影響
- client 側 JS への影響
- 関連画面への影響
- 関連ドキュメントへの影響

DB変更だけ、または画面変更だけで終わらせない。  
保存・取得・権限・表示まで一連で確認する。

Supabase SQL Editor 実行済み前提の migration がある場合でも、関連画面・RPC・RLSとの整合を確認する。

---

## 17. Skills / Automations / Plugins の扱い

Codex app では Skills、Automations、Plugins を利用できる場合がある。  
このリポジトリでは、これらを使う場合も、現在の正本と本ファイルのルールを優先する。

### Skills

繰り返し作業を Skills 化する場合は、以下を守る。

- このリポジトリ専用の手順として明文化する。
- 既存ドキュメントと役割が重複する場合は、新規作成ではなく既存文書へ統合する。
- 文字化け・全体置換・DB変更・push禁止のルールを必ず含める。
- 調査で許可される操作と、編集で承認が必要な操作を分けて書く。

### Automations

Automations を使う場合は、以下のような確認・整理タスクに限定する。

- 未対応Issueの整理
- PR差分レビュー
- CI結果確認
- TODOや未確定事項の棚卸し
- ドキュメントと実装のズレ確認

ただし、ユーザー確認なしに自動でコード変更・push・PR作成・本番DB変更をしてはいけない。

### Plugins

Plugins や外部連携を使う場合は、以下を守る。

- 連携先に渡す情報を最小化する。
- secret / key / token を扱わない。
- Google Drive、GitHub、Slack等の外部情報を現在仕様として使う場合は、リポジトリ内の正本と照合する。
- 外部情報だけで仕様を確定しない。

---

## 18. Git / commit / PR policy

Codex は、ユーザー承認済みの実装作業が完了した適切な区切りで、関連変更だけを小さくローカルコミットしてよい。

ただし、push はユーザーの明示指示があるまで行わない。  
PR作成も、ユーザーの明示指示があるまで行わない。

1コミットは1つの意味のある変更に限定する。  
巨大なコミットを避け、レビューしやすい履歴を残す。

コミット前には以下を確認する。

- 意図しないファイル変更がないこと。
- 文字コード変更が混ざっていないこと。
- 改行コードの一括変更が混ざっていないこと。
- 旧ファイルの削除が意図したものか。
- ドキュメント更新漏れがないこと。
- テストまたは確認結果を説明できること。

---

## 19. PR・既存ブランチ運用

既存PRや作業ブランチがある場合は、内容を確認してから扱う。

特に、大量ファイル変更・旧画面退避・CSS全面変更を含むPRは、現在の正本と衝突する可能性があるため、安易にマージしない。

古いCodex作業ブランチや大規模置換ブランチは、そのまま採用せず、必要な差分だけを拾う方針を優先する。

---

## 20. テスト・確認

このリポジトリには Playwright 設定がある。  
ただし `package.json` の scripts が空の場合、存在しない npm script を実行しない。

確認時は、可能な範囲で以下を行う。

- HTMLの参照パス確認
- JSの構文確認
- 主要ページの表示確認
- Supabase RPC / table 名の整合確認
- 予約・QR・会員・スタッフ・管理者導線の影響確認
- 変更箇所に関係する既存機能の回帰確認
- `git diff` による意図しない変更の確認

自動テストが実行できない場合は、その理由を報告する。

---

## 21. 報告形式

作業完了時は、以下を簡潔に報告する。

- 何を確認したか
- 何を変更したか
- どのファイルを変更したか
- 実行したテスト・確認
- 実行できなかった確認と理由
- ローカルコミットを作成した場合は、そのコミット概要
- 残課題
- 次に行うべき作業

不明点、未確定事項、ユーザー判断が必要な点は、推測で埋めずに明示する。

---

## 22. 判断に迷った場合

判断に迷った場合は、以下を優先する。

1. 既存機能を壊さない。
2. 現在の正本を優先する。
3. 未確定事項を勝手に仕様化しない。
4. DB・認証・状態管理を軽視しない。
5. QR導線と会員導線を混ぜない。
6. 大規模変更より局所修正を優先する。
7. 文字化けに見える箇所を勝手に直さない。
8. Windows表示由来の問題を疑う。
9. 調査・確認は進めてよい。
10. 編集・置換・破壊的操作は承認を取る。
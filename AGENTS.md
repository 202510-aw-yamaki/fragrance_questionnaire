# Codexへの指示書 (AGENTS.md)

## 1. プロジェクトの概要
- **あなたの立場**
	- 主体者はユーザー、あなたはコーディングアシスタント
	- ユーザーは案としてほしいことを発言し、あなたがそれに合うコーディングをしていく
	- 修正は一部置換が基本、全体置換をする際はユーザーに要確認。
ここでいう全体置換は1ファイル内での全体置換も含む。
	- 文字化けを感じた場合でも必ずユーザーに確認
	- 文字化けはパワーシェルのエンコード由来。ユーザーには文字化けなく見えておりコードも正常。
	- それでも気になる場合は要ユーザー確認
	- ユーザーからの指示に対して、まずは意図確認。勝手に修正しない。
	- 「まず確認、承諾を得てから着手」を徹底する。


# 重要
文字化けはパワーシェルのエンコード由来。ユーザーには文字化けなく見えており、コードも正常。
UTF-8、BOMはつけてはいけない。
修正は一部置換が基本、全体置換をする際はユーザーに要確認。
ここでいう全体置換は1ファイル内での全体置換も含む。
インシデントが発生した際はマークダウンに残し、対応も記述。
作業の項目を作成したマークダウンの指示作業を終えた後は、中身は変えずに追記する形で運用すること。
チェックリストがある場合は、作業終了後チェックボックスにチェックを入れ、下の行に「→」+「内容」の記述。もともとの作業分は変えない。
マークダウンに仕様変更の記述をする際は元の元記述は残し、その後どういう要望で変更したのかをわかる形で追記する。
ユーザーからの指示に対して、まずは意図確認。勝手に修正しない。
「まず確認、承諾を得てから着手」を徹底する。

## Commit policy

このリポジトリでは、Codex は作業中に適切な区切りで Git コミットを作成してよい。
ただし、push はユーザーの明示指示があるまで行わない。

1コミットは1つの意味のある変更に限定する。
巨大なコミットを避け、レビューしやすい履歴を残す。

## 各 Markdown の役割
- `docs/SPEC.md`
  ユーザー要望の仕様固定書です。仕様として古くなった記述は更新してください。
- `docs/SURVEY_SCORING_LOGIC.md`
  アンケートのポイント付けと分岐条件はここにしるしてください。
- `docs/MATERIAL_POINTS.md`
  各原料ごとの現段階での保有ポイントテンプレートです

必要以上に Markdown を増やさないでください。  
役割が既存文書と重なる場合は、新規作成ではなく既存文書への追記・更新・統合を優先してください。

# この先の考え

## Project Core

このプロジェクトは、香りワークショップ向けの予約・アンケート・スタッフ管理・QR商品導線システムである。

最優先の目的は、アンケート結果・香りロジック・DB・スタッフ運用を安定させること。
その上で、ワークショップで作成された香水をQR商品として第三者が作成依頼できる導線を追加する。

## 設計資料の優先順位

Codex は作業前に、必ず以下を読むこと。

1. AGENTS.md
2. docs/00_PROJECT_CORE.md
3. docs/01_CURRENT_STATE.md
4. docs/02_TARGET_ARCHITECTURE.md
5. docs/03_DB_DESIGN_POLICY.md
6. docs/04_QR_PRODUCT_FLOW.md
7. docs/05_IMPLEMENTATION_ROADMAP.md
8. docs/06_OPEN_ISSUES.md
9. docs/presentation/fragrance_questionnaire_architecture_v5.pptx

特に、DB・認証・QRコード関連の作業では、03_DB_DESIGN_POLICY.md と 05_IMPLEMENTATION_ROADMAP.md を必ず参照すること。
そして、06_OPEN_ISSUES.md にある項目は、勝手に実装しないこと。

## 仕様資料の扱い

既存の `docs/SPEC.md`、`docs/SURVEY_SCORING_LOGIC.md`、`docs/MATERIAL_POINTS.md` は、既存仕様や個別ロジックの参照資料として扱う。

ただし、今後の全体設計・実装順序・DB/QR方針については、以下の `00`〜`06` の設計資料を優先する。

特に QR 商品導線、DB設計、認証・権限、実装順序については、`docs/00_PROJECT_CORE.md` 〜 `docs/06_OPEN_ISSUES.md` を優先する。

## Non-Negotiable Rules

1. UIだけを先に作って、DB・認証・状態管理を後回しにしない。
2. QRコード関連の古い実装を曖昧に残さない。
3. QR商品導線と会員導線を混ぜない。
4. QRから来た第三者を会員DBに入れない。
5. QRコードは「元のお客様の完成品」に紐づく。
6. QR商品依頼は「購入」ではなく「作成依頼」として扱う。
7. staff-customer-detail.html には、商品名・個人情報同意・第三者作成同意を追加する。
8. 管理者・スタッフの権限を混在させない。
9. DB変更時は必ず migration / schema / 型 / 画面影響を確認する。
10. 実装後は、関連ドキュメントも更新する。

## Implementation Order

原則として、以下の順で実装する。

0. 現状監査
   - 既存コード、DB、認証、QR関連処理、画面構成を確認する
   - この段階ではコード変更しない
   - 設計資料との差分を整理する
1. DB・認証・権限・既存データ整理
2. アンケート・ロジック・原料ポイント調整機能の安定化
3. 会員登録DB接続と会員ページ整理
4. staff-customer-detail.html の追加項目実装
5. QR商品作成依頼導線
6. 管理者設定・通知・期限管理
7. UI改善・仕上げ

## QR Product Rules

- QR商品ページでは商品名のみ表示する。
- 表示する入力項目は、10ml数量、30ml数量、メールアドレス。
- ボタン文言は「この香水を作成依頼する」。
- 送信後は受付メールを自動送信する。
- スタッフは3営業日以内に作成可能 / 作成不可を判断する。
- 3営業日を超えて未対応の場合は、自動で作成不可メールを送信する。
- 期限超過未対応は管理者側に記録し、店舗体制の問題として集計する。
- 作成可能メール送信後、3日後に再案内メール、7日後に期限切れとする。
- 発送完了時点でスタッフ成果Bとしてカウントする。
- QR商品ページは最後の発送完了から半年注文がなければ無効化する。
- 無効化後にアクセスが多い場合は、管理者通知に出す。

## Current Known Issues

- 会員ページからアンケート経由で予約した際、前回完成品との差分表示がない。
- 商品予約ページが未作成。
- QR商品価格設定が管理者画面にない。
- 会員登録のDB接続が未完了。
- スタッフ自動ログインにより、管理者ログイン時に一度ログアウトが必要になる。
- staff-customer-detail.html に商品名入力欄がない。
- 個人情報同意・第三者作成同意のDB保存が未整備。

## Archive の扱い

`docs/archive/` 配下の Markdown は、過去の実装指示・作業ログとして扱う。

現在の仕様・設計・実装順序の正本ではない。

Codex は、`docs/archive/` 配下の内容を現在仕様としてそのまま実装してはいけない。
必要な場合のみ、現在の `docs/00`〜`docs/06` と照合して参照すること。

`archived/legacy/` 配下は、リニューアル後に旧HTMLページ・旧JS・旧CSSなどの過去ページ実体を隔離する場所として扱う。

`archived/legacy/` 配下の内容は、現在仕様・現在実装の参照対象ではない。
Codex は、ユーザーから明示的な指示がない限り、`archived/legacy/` 配下の内容を現在仕様として参照・流用・実装してはいけない。

新規ページへのリニューアルが完了し、旧ページを残す必要がある場合は、削除ではなく `archived/legacy/` へ移動して、現行導線と混在しないようにする。

# Current State

## 2026-05-04 Latest Staff Page State

この節は 2026-05-04 時点のスタッフ系ページの最新状態です。下部に古い課題として `staff-customer-detail.html` に商品名入力欄がない等の記述が残っている場合でも、現在は本節を優先してください。

- `staff/staff-slots.html`
  - スタッフ専用CSSは `css/staff-ui.css` に集約済み。
  - ヘッダーはスタッフダッシュボード基準。通知リンクは除去済み。
  - PC は週カレンダー、予約枠作成フォーム、作成済予約枠一覧の3カラム。
  - タブレットでは週カレンダーとフォームを `1:2` 目安で横並びにし、フォーム高は週カレンダーへ近づけた。
  - 予約枠追加/まとめて作成はいずれも確認導線あり。まとめて作成はモーダル。

- `staff/staff-reservations.html`
  - ヘッダーは `ダッシュボード` 導線へ整理済み。
  - 完了済み切替は予約一覧行の右側に配置済み。
  - メール列は削除済み。

- `staff/staff-customer-detail.html`
  - 見本寄せのレイアウトへ再構成済み。旧画面の流用前提ではなく、スタッフ確認用とお客様共有アクションを分離した。
  - 予約時の名前/メールは `reservations.customer_name` / `reservations.customer_email` を正として表示する。
  - ゲスト予約は Supabase Auth セッションが残っていても会員情報に引っ張られない。ログイン予約のみ会員登録状況へ紐づく。
  - サマリーは折りたたみ可能。電話番号はサマリーに出さない。
  - スタッフ確認用にはアンケート5軸、来店前メモ、おすすめ配合を表示する。
  - 来店前メモは表示専用。編集欄ではない。
  - 事前配合調整を追加済み。提案配合は DB 保存し、設定済み/未設定の状態だけをカードに表示する。
  - お客様と香りを調整するモーダルは、提案配合を初期バランスとして使い、決定時に最終配合へ反映する。
  - 商品名と商品タグ選択モーダルを追加済み。商品タグは管理者設定候補から選ぶ。
  - 最終操作は `最終登録` と `QR付きメールを送る` を横並びにする。途中保存、固定の状態表示 p、下部大見出しは不要。

- DB / Migration
  - 予約者情報保存: `supabase/migrations/20260504120000_reservation_contact_persistence_guard.sql`
  - 事前配合と商品タグ: `supabase/migrations/20260504150000_staff_previsit_product_tags.sql`
  - QR商品RLS再帰修正: `supabase/migrations/20260504154500_fix_fragrance_product_policy_recursion.sql`
  - 上記 SQL はユーザー側で SQL Editor 実行済みの前提で進めてよい。

## Purpose

このファイルは、現在の実装状況と未解決課題を整理するためのもの。
Codex は実装前にこのファイルを読み、既存機能を壊さず、現状課題を見落とさないこと。

## 現在ある主な画面

- `index.html`
  - トップページ
  - 初見客向けアンケート導線
  - 会員ログイン導線
  - 管理者ログイン導線はフッター付近に存在

- `customer/questionnaire.html`
  - 初回アンケート画面
  - 5問構成

- `customer/questionnaire_step2.html`
  - 分岐後アンケート画面
  - 分岐2問＋共通1問

- `customer/fragrance-graph.html`
  - 香りグラフ可視化ページ

- `customer/reservation.html`
  - 来店予約ページ

- `customer/customer-login.html`
  - 会員ログインページ

- `customer/index.html`
  - 会員トップページ

- `customer/product-reservation.html`
  - 商品予約ページ想定
  - 現状は未完成・未実装扱い

2026-04-29追記:
`customer/product-reservation.html` は、`product_qr_codes` と `fragrance_products` に紐づくQR商品作成依頼ページとしてリニューアル済み。
ただし、作成可能/不可判断、発送先入力、発送完了、通知対応済み操作は後続フェーズとして扱う。

- `admin-login.html`
  - 管理者ログインページ

- `staff/staff-customer-detail.html`
  - スタッフが来店客のアンケート結果・予約情報・ヒアリング内容・最終配合を確認、入力する画面

## 現在できている重要機能

- アンケート回答
- アンケート結果の保存
- 香り5軸グラフの表示
- 来店予約
- スタッフ側での予約・アンケート確認
- 最終原料・割合の登録
- 管理者側でのロジック調整
- 管理者側での原料ポイント調整
- JSON出力・読込によるロジック保持

## 現状の課題

### 会員機能

- 会員登録のDB接続が未完了。
- 会員ページからアンケート経由で再予約した場合、前回完成品との差分を見せる作りになっていない。
- 会員ID・パスワード・メールアドレス・アンケート結果・制作履歴の紐づけがまだ弱い。

2026-04-29追記:
会員ログインページは Supabase Auth signUp / signIn と `customers.auth_user_id` 作成に接続した。
ただし、会員本人の制作履歴表示、過去完成品との比較、アンケート結果・予約・制作履歴の完全な紐づけは未完了のため、上記課題は「DB接続の入口は実装済み、会員ページ整理は継続」として扱う。

2026-04-29追記:
ログイン済み会員の `customers.id` を、アンケート結果・予約・完成品へ紐づける土台を追加した。
ただし、会員ページ上での制作履歴表示、過去完成品との差分表示、過去完成品を起点にした再予約UIは後続フェーズとして扱う。

2026-04-29追記:
`customer/index.html` を会員ページとして追加し、Supabase Authログイン中の会員本人に限定して予約履歴・制作履歴を表示する入口を作成した。
表示項目は限定RPCで取得し、過去完成品との差分表示や再予約UIは引き続き後続フェーズとして扱う。

### 商品・QR導線

- 商品予約ページが未作成。
- QRコードからの商品作成依頼導線が未整備。
- QR商品価格を管理者画面で設定する機能がない。
- QRコードと完成品データの紐づけ方が曖昧なままだと将来的なアキレス腱になる。

2026-04-29追記:
QR商品作成依頼ページ、QR公開設定、QR依頼保存、受付通知、受付メールイベント、アクセス記録の入口は実装済み。
作成可否判断、発送先入力、発送完了、通知対応済み操作、無効化QRのアクセス急増通知は後続フェーズとして扱う。

2026-04-29追記:
スタッフ/管理者向けにQR依頼の読み取り専用一覧を追加した。
依頼者メールはマスク表示に留め、作成可否判断や通知対応済み操作はまだ実装していない。

### スタッフ画面

- `staff-customer-detail.html` に商品名入力欄がない。
- 個人情報同意のチェック保存がない。
- 第三者作成同意のチェック保存がない。
- QR発行条件が明確に実装されていない。

2026-04-29追記:
商品名、個人情報同意、第三者作成同意はスタッフ画面から保存できる状態にした。
QR発行条件、QR依頼対応、発送先入力、発送完了、通知対応済み操作は後続フェーズとして扱う。

### 認証・権限

- スタッフの自動ログインが有効なため、管理者がスタッフとしてログインするには一度ログアウトが必要。
- 管理者・スタッフ・会員の権限分離が十分ではない。
- UI上だけでなくDB側でも権限を分ける必要がある。

## 実装時の注意

- 既存画面は可能な範囲で活かす。
- ただし、DB・認証・QRコード関連の曖昧な構造は放置しない。
- 見た目だけを整える実装を優先しない。
- 既存のQR関連処理がある場合は、流用前に必ず設計方針と照合する。

## 2026-05-01 UI再構築後の現状

- 旧HTMLページと旧レイアウトCSSは `archived/layout-rebuild-20260501/` に退避済み。
- 現行HTMLは `レイアウトimg/` の参照画像を基準に再構築し、共通CSSは `css/rebuild-ui.css` に統合した。
- `supabase/`、`js/supabase-config.js`、`js/supabase-client.js`、`js/public-data.js`、`js/admin-data.js`、`js/admin-auth.js`、`js/fragrance-master-data.js` は接続・保存・認証の維持対象として残した。
- 顧客アンケート、結果、予約完了の軽量な画面制御は `js/rebuild-customer-flow.js` に整理した。
- 会員ログイン/会員トップの画面制御は `js/customer-auth-page.js` に整理した。
- 共通ログイン画面のタブ切替と送信制御は `js/admin-login-page.js` に整理した。
- Edge/Playwrightで20ページをデスクトップ幅・スマホ幅の計40表示確認し、ローカルJSエラーなしを確認済み。

残作業:
- Supabase実接続状態でのログイン、保存、予約、QR依頼送信の結合確認。
- QR依頼の作成可否判断、通知、メール、期限管理の運用実装。
- QR発送入力、発送完了、成果集計、期限超過集計の実装。

## 2026-05-03 来店予約ページ更新後の現状

- `customer/reservation.html` は、参照画像に合わせたカレンダー型の予約枠選択UIへ更新済み。
- 公開予約枠は `reservation_slots` から取得し、予約枠がある日付だけ選択可能にし、選択日の時間枠を `slot-list` に表示する。
- 予約ページ上では香りバランス等を表示しないが、予約確定payloadでは `questionnaire_result_id`、`axes`、`summary_*` を維持する。
- 来店予約時の名前とメールアドレスは `reservations.customer_name` / `reservations.customer_email` に保存する。
- 予約完了ページとスタッフ詳細ページは、保存済みの名前・メールを参照できる。

2026-05-03追記:
予約完了ページは `レイアウトimg/10. customer reservation-complete.html 予約完了ページ.png` に合わせ、吟ロゴヘッダーと完了カードの構成へ更新済み。
予約完了メッセージには `reservations.customer_name` を表示し、所要時間は予約時に算出した `reservations.duration_minutes` を表示する。

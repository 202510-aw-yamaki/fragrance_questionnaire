# SPEC

## 2026-05-04 Staff Pages Current Source Of Truth

この節は、2026-05-04 時点でユーザー確認を受けたスタッフ系ページの正本仕様です。既存の古い記述に `staff-customer-detail.html` の商品名入力不足などが残っている場合でも、この節を優先してください。

### staff/staff-slots.html

- スタッフ系ページは `assets/css/staff-ui.css` に集約する。`assets/css/rebuild-ui.css` 依存へ戻さない。
- ヘッダーはスタッフダッシュボードと揃える。通知リンクは表示しない。`SLOTS` の右にスタッフ名を置き、ページ導線は `ダッシュボード` を含める。
- 画面は PC で左から `週カレンダー`、`予約枠作成フォーム`、`作成済予約枠一覧` の3カラム。
- タブレット幅では週カレンダーとフォームを横並びにし、おおむね `1:2` の比率にする。
- 週カレンダーは `前` / `次` で週移動できる。日付ボタンは高さを詰め、横スクロールを出さない。
- フォームは日付/定員、開始時刻/枠時間を2列配置にする。フォーム高さは週カレンダーに近づけ、余白で調整する。
- `予約枠を追加` と `まとめて作成` は横並び1ロウ。単体追加とまとめて作成はいずれも確認ダイアログを出す。
- まとめて作成はモーダルで行う。開始日/終了日は同一行、開始時刻/枠間隔/定員は同一行にまとめる。
- 注釈テキストやプレビュー用の不要な p テキスト、スタッフ名/公開中のプレビュー表示、一覧見出しの日付 span は表示しない。
- 作成済み予約枠一覧の日付は西暦を省き、月/日表示にする。

### staff/staff-reservations.html

- ヘッダーから `予約確認` を外し、`ダッシュボード` を入れる。
- `完了済みを隠す` の切替は予約一覧行の右端に配置する。
- メール欄はこのページでは不要なため表示しない。

### Reservation Contact And Customer Linkage

- 顧客が予約枠へ予約する時点で入力する `名前（ニックネーム可）` と `メールアドレス` は予約データとして保存する。
- 保存先は `reservations.customer_name` / `reservations.customer_email` を正とする。
- 接客詳細では、まず予約時入力値を表示する。会員プロフィールの名前/メールへ安易にフォールバックしない。
- 同じブラウザに Supabase Auth セッションが残っていても、ゲストとして予約する導線では別人予約に会員情報を引きずらない。
- ログイン予約の場合のみ、その予約に会員登録状況を紐づける。ゲスト予約は `未登録` として扱う。

### staff/staff-customer-detail.html Layout

- ヘッダーは他スタッフページと揃え、ロゴは `吟` のみのロゴを使う。
- 上部サマリーは折りたためる構造にする。基本表示は `お客様名`、`お客様情報を入力`、`同意内容を確認する`、詳細開閉ボタンを横並びにする。
- 電話番号はサマリーには出さない。電話はお客様情報モーダル内の任意入力として扱う。
- ステータスチップは上部の空いたスペースに収める。下に落ちる配置は避ける。
- `お客様情報を入力` モーダルは、名前とメールを同一行、電話と保存を同一行に近い構成にする。個人情報確認済みチェックはこのモーダルでは不要。

### Staff Confirmation Area

- `スタッフ確認用` の括弧内説明は不要。
- アンケート結果には今回の5軸グラフのみを載せる。分岐、仕上げ、Q1-Q8 の羅列は出さない。
- 来店前メモは顧客が事前入力した内容の表示専用。DBデータがない場合は `未記載` と表示し、長文はスクロールで確認できるようにする。
- おすすめ配合は来店前メモの下に表示する。
- 当日ヒアリングメモとお客様感想メモはスタッフ確認用エリアから外し、`お客様と香りを調整する` モーダル内で扱う。
- 途中保存ボタンはスタッフ確認用エリアには置かない。

### Previsit Recipe Proposal

- `事前配合調整` はお客様共有アクションの上に置く。
- ここでは来店前の提案配合を作る。設定済みの場合、画面上は配合明細を表示せず `配合提案は設定済み` と表示する。未設定時は `提案配合は未設定です`。
- 事前配合調整モーダルは、アンケート結果の5軸と提案配合による5軸を並べて表示する。
- 原料と割合に応じて提案配合の5軸をリアルタイム更新する。
- 割合は数値入力とスピンボタンに加え、下部スライダーで調整できる。
- 原料ごとの保有ポイントだけをそのまま表示値にせず、アンケート結果の5軸と見え方が破綻しないよう補正した5軸値を使う。
- 5軸の数値チップは、L字型にグラフを抱える配置にする。
- 提案配合は DB に保存する。保存先は `workshop_sessions.previsit_recipe_items` と `workshop_sessions.previsit_recipe_axes`。

### Customer Shared Actions

- 共有アクションは `お客様と香りを調整する` と `商品名を決める` の2つ。
- 各ボタンの small 説明文は表示しない。アイコンは見本に近い簡素なアイコンを使う。
- `お客様と香りを調整する` モーダルは、事前配合調整モーダルに近い構成にする。
- 香り調整モーダルの現在のバランスには、提案配合が設定済みなら提案配合の5軸を表示する。
- 香り調整モーダルの下部見出しは `一緒に調整しましょう`。`おすすめを追加` は置かない。
- 香り調整モーダルには、当日ヒアリングメモとお客様感想メモを半々で置く。
- `これで決定` を押した場合だけ、調整後の配合を最終配合へ反映する。`×` で閉じた場合は反映しない。
- `商品名を決める` モーダルのタイトルは `香水の名前とタグを選びましょう`。
- 商品タグは管理者設定から読み込む候補をクリック選択する。12種類程度の候補から、商品ページ用に最大3種類程度を選ぶ想定。
- 選択した商品タグは `fragrance_products.product_tags` に保存し、QR商品ページ表示に使う。

### Final Operation Area

- 下部の `スタッフ操作・ワークショップ運用` 見出しは表示しない。
- 最終配合の明細カードは原則不要。最終配合は `お客様と香りを調整する` の `これで決定` で反映される状態を正とする。
- `途中保存` ボタンは置かない。完了操作は `最終登録` のみ。
- `QR付きメールを送る` は `最終登録` の右に横並びで置く。
- ステータスサマリーは QR発行状況、メール送信状況、ワークショップ状態を横に読みやすくまとめる。
- `save-status` のような状態表示 p テキストは、画面内の固定情報としては不要。エラー時の内部状態表示に戻さない。

### DB / RLS Notes

- 予約者情報保存の SQL は `supabase/migrations/20260504120000_reservation_contact_persistence_guard.sql`。
- 事前配合と商品タグの SQL は `supabase/migrations/20260504150000_staff_previsit_product_tags.sql`。
- QR商品RLS再帰修正の SQL は `supabase/migrations/20260504154500_fix_fragrance_product_policy_recursion.sql`。
- SQL Editor で `fetch_reservation_by_code(text)` の戻り値変更エラーが出る場合は、関数を drop してから再作成する migration の版を使う。
- `fragrance_products` と `product_qr_codes` のRLSで相互参照を直接書くと `infinite recursion detected in policy for relation "fragrance_products"` が出るため、Security Definer 関数経由にする。

本書は、今後このリポジトリで目標とする仕様の固定書です。  
配点設計と原料プロファイル設計の基準資料は `docs/SURVEY_SCORING_LOGIC.md` とし、実装の進捗管理は現行の `docs/` 配下資料で行います。

## 正本の考え方

- 配点ロジックと原料プロファイルの基準資料は `docs/SURVEY_SCORING_LOGIC.md`
- 目標仕様の整理先は `docs/SPEC.md`、`docs/IMPLEMENTATION_RULES.md`、`docs/SURVEY_SCORING_LOGIC.md`、`docs/MATERIAL_POINTS.md`
- 実装済み / 未達の状態管理は `docs/COMPLETED_REQUIREMENTS.md`、`docs/UNMET_REQUIREMENTS.md`
- 画面構成は現行HTML/CSS/JSを正本として扱う
- 数値仕様が必要な場合は、必ず `docs/SURVEY_SCORING_LOGIC.md`、`docs/MATERIAL_POINTS.md` を参照する
- 現行コードが仕様書と異なる場合は、原則としてコード側が未追従とみなす

## システムの到達目標

- 顧客側は現状のフロント導線を大きく崩さず使えること
- 顧客側の保存・取得は Supabase を主経路とし、取得失敗時でも `sessionStorage` や demo data で画面遷移が止まらないこと
- 店舗スタッフが IT に強くなくても、予約枠作成、予約確認、来店前準備、ワークショップ結果登録、レシピ登録まで扱えること
- 店舗管理者が、スタッフ権限、基剤、原料、将来のテンプレート拡張を管理できること
- バックエンドは Supabase を使用すること
- 再注文時に過去レシピと五軸グラフを参照でき、蓄積結果を将来のおすすめテンプレートへ反映できること

## 現行サイト構成

- 顧客向け入口は `index.html`
- 顧客向けページは `customer/` 配下に置く
  - `customer/questionnaire.html`
  - `customer/questionnaire_step2.html`
  - `customer/fragrance-graph.html`
  - `customer/reservation.html`
  - `customer/reservation-complete.html`
  - `customer/product-reservation.html`
- スタッフ / 管理者の共通ログインは `admin/login.html`
- スタッフ向けページは `staff/` 配下に置く
  - `staff/staff-dashboard.html`
  - `staff/staff-customer-detail.html`
  - `staff/staff-slots.html`
  - `staff/staff-reservations.html`
- 管理者向けページは `admin/` 配下に置く
  - `admin/admin-dashboard.html`
  - `admin/admin-settings.html`
  - `admin/admin-scoring.html`
  - `admin/admin-materials.html`
- `admin/login.html` はスタッフ / 管理者専用のため、フッターに小さく導線を置く方針へ変更する
- スタイル管理は現段階では各HTML内のインラインスタイルを正とし、共通CSS整理は将来対応とする

### 追加予定:

- customer/login.html
- customer/index.html
- customer/shipping-info.html
- admin/admin-qr-settings.html

## 役割

### A. 顧客側

- 現状の FrontPage 群をそのまま使う
- 対象ページ
  - `index.html`
  - `customer/questionnaire.html`
  - `customer/questionnaire_step2.html`
  - `customer/fragrance-graph.html`
  - `customer/reservation.html`
  - `customer/reservation-complete.html`
  - `customer/product-reservation.html`
- 取得したい情報
  - アンケート回答
  - アンケート計算後の五軸
  - 予約時点で確定した五軸
  - 予約情報
  - 顧客識別用の `customer_id`
- 顧客向けページのデータ取得 / 保存は、Supabase を優先しつつ `sessionStorage` フォールバックを持つ

### B. 店舗スタッフ側

- 共通ログインページ `admin/login.html` から入る
- 現行の対象ページ
  - `staff/staff-dashboard.html`
  - `staff/staff-reservations.html`
  - `staff/staff-slots.html`
  - `staff/staff-customer-detail.html`
- 出勤シフトに応じた予約枠作成
- 予約状況確認
- 来店前の顧客情報確認
  - 予約情報
  - 事前アンケート回答
  - 五軸グラフ
- 店舗での対話後に確定した最終五軸グラフの入力
- 商品作成時のレシピ登録
  - 基剤
  - 原料
  - 各割合
  - ロット
  - 商品種

### C. 店舗管理者側

- 共通ログインページ `admin/login.html` から入る
- 現行の対象ページ
  - `admin/admin-dashboard.html`
  - `admin/admin-settings.html`
  - `admin/admin-scoring.html`
  - `admin/admin-materials.html`
- 管理者画面への登録権限 / 編集
- 店舗スタッフ権限の登録 / 編集
- 基剤登録 / 編集
- 原料登録 / 編集
- 将来のおすすめテンプレート管理

## 管理画面の考え方

- UI 上は「店舗スタッフ向け機能」と「店舗管理者向け機能」を分ける
- ただしログイン導線は共通管理ログインでよい
- ファイル配置は `staff/` と `admin/` を分け、共通ログインのみルートの `admin/login.html` に置く
- 権限はロールで分ける
  - `staff`
  - `admin`

## Supabase で扱う情報

- 公開ページは Supabase を主経路として利用する
- ただし中間ページ直開きや一時的な取得失敗に備え、`sessionStorage` や demo data のフォールバックを併用する

### 顧客・予約・結果

- `customer_id`
- 予約情報
- 事前アンケート回答
- 五軸グラフ
  - アンケート計算直後
  - 予約時点
  - 店舗調整後の最終値
- スタッフメモ
- 再注文履歴

### レシピ

- 店舗で確定した五軸グラフ
- 使用した基剤
- 使用した原料
- 各割合
- ロット
- 商品種
- 五軸との紐付け

### マスタ

- 原料マスタ
- 将来のおすすめテンプレート候補
- スタッフ / 管理者権限

## 原料の扱い


- 数値管理は「単位量あたり五軸へ与える値」で扱う

## 最終成果物の定義

- 店舗スタッフが迷わず使える粒度のページ構成
- Supabase を用いたバックエンド
- スタッフがシフトに応じて予約枠を作成できること
- 予約状況や事前アンケートを見て来店前準備ができること
- ワークショップで出来上がった内容を登録できること
- レシピと五軸グラフの紐付け粒度を高く保てること
- 再注文実績が増えたものを将来のフロントおすすめテンプレートに追加できること
- QR コードは商品の作成依頼ページと紐づき、顧客の顧客からの注文を受けれること

### QRコード導線

- QRコードの初期実装の遷移先は、`customer/product-reservation.html` とする
- `customer/product-reservation.html` は、QR経由の第三者が会員登録なしで商品作成依頼を行うページとして扱う
- QRコードは会員本人ではなく、ワークショップで完成した香水に紐づける
- QR商品導線は、会員導線とは分けて扱う

## ページリニューアル時の保持・破棄方針

- 捨ててよいもの:
  - 既存ページの見た目用HTML構造
  - 過剰に重なったCSS
  - 表示都合だけのJS
- 残すべきもの:
  - Supabase接続
  - 認証
  - RLS前提のデータ関数
  - アンケート配点ロジック
  - `sessionStorage` の契約
  - 既存DOM IDのうちDB連携JSが参照するもの
- 補足:
  - 現行HTML/CSS/JSを正として、ページ単位の差分を確認する。
  - 新ページでDB連携が維持できることを確認した後、不要な旧HTML/CSS/表示都合JSは公開用リポジトリへ含めない。

## 2026-05-03 会員比較モードのページ分離方針

- 通常結果ページは `customer/fragrance-graph.html` とし、今回のアンケート結果の表示・5軸調整・予約への受け渡しを担当する。
- 会員比較モードは `customer/fragrance-compare.html` とし、会員ログイン済みで前回完成品の5軸データがある場合に、前回完成品と今回アンケート結果を比較するページとして扱う。
- 通常結果ページに `mode=compare` のような状態を混在させず、ページを分けることで通常結果、未回答テンプレート、会員比較の責務を分離する。
- 会員ページの比較導線は `customer/fragrance-compare.html` に向ける。
- アンケート完了時は、会員ログイン済みかつ比較可能な前回完成品データがある場合のみ `customer/fragrance-compare.html` へ遷移し、それ以外は `customer/fragrance-graph.html` へ遷移する。
- レーダー描画、5軸名称、予約へ渡す `sessionStorage` の契約は通常結果ページと共通の考え方を使う。
- 2026-05-03追記: 会員比較ページは右側の固定コメントカードを置かず、詳細説明は下部の「詳細を確認する」ボタンから開くモーダルに表示する。
- 2026-05-03追記: 会員比較ページの下部アクションには「この香りで予約する」「通常結果ページへ戻る」「詳細を確認する」を並べる。
- 2026-05-03追記: 会員比較ページでは今回結果の5軸をスライダーで調整でき、調整値は通常結果・予約へ渡す値に反映する。
- 2026-05-03追記: 会員比較ページの上段カード内には5軸数値リストを出さず、前回側の日付表示欄は日付がない場合に「表示できる５軸データがありません。」を表示する。
- 2026-05-03追記: 会員比較ページの3アクションはフッター独立配置ではなく、5軸スライダー左側のボタンカラムとして配置する。
- 2026-05-03追記: 会員比較ページの5軸スライダーは「軸名・スライダー・今回値」の1行表示とし、前回値や差分値は表示しない。
- 2026-05-03追記: 会員比較ページの縦積み表示では今回のアンケート結果を先に表示し、前回完成品の5軸データがない場合は前回カードを非表示にする。
- 2026-05-03追記: 会員比較ページで前回完成品の5軸データがない場合、ページ見出しは「今回のアンケート結果」とし、比較説明文は非表示にする。
- 2026-05-03追記: 会員比較ページの今回グラフ直下には「初期値に戻す」ボタンを置き、今回結果の5軸をアンケート完了時点の初期値へ戻せるようにする。
- 2026-05-03追記: 詳細モーダルでも縦積み表示では今回結果を先に表示し、前回完成品の5軸データがない場合は前回グラフを非表示にする。

## 2026-05-06 おすすめ配合ロジックの実装範囲

- スタッフ顧客詳細の「おすすめ配合」は、8問回答パターンから算出した5軸に対して、3原料×5%刻み比率の候補から最適1件を表示する。
- 商品ページやQR導線では配合割合を表示しない。今回のおすすめ配合表示はスタッフ確認・接客調整用に限定する。
- 事前計算結果は `recommendation_recipe_cache` に保存できる。未保存の場合は同じ最適化を画面側で計算し、保存可能な場合はキャッシュへ戻す。
- 最終配合は従来通りスタッフが接客で調整した内容を `workshop_sessions.recipe_items` に保存する。
- 法規制約、Top 3表示、多様化、秘匿配合分離、QR再注文拡張は今回の主実装に含めない。

## 2026-05-06 予約通知・過予約防止の実装範囲

- 顧客予約は `create_public_reservation()` RPC 経由で保存し、DB側で無効枠、受付終了枠、過去枠、定員超過を防止する。
- 予約枠の定員は `reservation_slots.capacity` を正とし、`reservations.status <> 'canceled'` を枠使用中として数える。
- 顧客予約画面では `slot_full` / `slot_closed` / `slot_past` を日本語メッセージへ変換し、予約完了画面へ遷移せず枠を再読込する。
- 予約作成時は `notification_events.event_type = 'reservation_created'` を作成し、スタッフダッシュボードに予約日時、お客様名、香り傾向、詳細リンクを表示する。
- 担当スタッフ判定は `reservation_slots.staff_profile_id` を優先し、既存データ向けに `instructor_name` を補助的に使う。
- スタッフが予約詳細を開いた時点で、該当予約通知は `seen` に更新する。
- 管理者ダッシュボードではスタッフ別に本日枠数、本日予約数、今週予約数、接客完了数、来店前提案未完了数、未確認予約通知数を表示する。
- 通知はアプリ内通知のみとし、メール、LINE、ブラウザPush、詳細な操作監査ログは今回の範囲外とする。

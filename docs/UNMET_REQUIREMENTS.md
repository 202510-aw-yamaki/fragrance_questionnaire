# UNMET_REQUIREMENTS

本書は、再編元 Markdown の構想のうち、現行コードにまだ接続されていない項目、または将来拡張として残っている項目だけを整理したものです。

## 本書の扱い

- 本書は「まだ正として採用していない構想」だけを残す
- 本書にある内容は、現時点のアプリ挙動を表すものではない
- 実装済みか迷う項目は、まず `COMPLETED_REQUIREMENTS.md` と実装コードを確認する

## 原料提案ロジック

- `material_points` や 16 原料プロファイルを使って、公開導線上で原料候補やブレンド候補を自動提案する処理は未実装
- `deep-research-report.md` にあった Top / Middle / Last の構成を使う原料ブレンド探索は未実装
- 5 軸ターゲットから原料比率へ落とす正規化計算や距離最適化は未実装

## 校正フロー

- ワークショップ後に「作った香り」の 5 軸主観評価を回収する導線は未実装
- 回収した実データを使って原料プロファイルを回帰校正する仕組みは未実装
- 回答 delta を回帰で見直す仕組みは未実装

## 原料ポイントの公開導線接続

- 現在の `material_points` は管理用 CRUD までは入っているが、`questionnaire` / `graph` / `reservation` の結果と結び付けていない
- `material_points` は将来拡張用データとして保持されているが、現段階では推薦文や配合候補の生成には使っていない

## 運用上の宿題

- 研究由来の数値は仮説パラメータとして持ち、将来の運用で見直せる構造を維持する
- 実装済みの配点ロジックと研究メモ上の拡張案が混在しないよう、将来拡張時は `SURVEY_SCORING_LOGIC.md` と `MATERIAL_POINTS.md` を合わせて更新する
